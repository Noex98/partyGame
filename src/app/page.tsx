import { Logo } from '@/components';
import React from 'react'

export const metadata = {
    title: 'Home',
    description: 'idk man',
};

export default function page() {
  return (
    <div>
        <Logo/>
    </div>
  )
}

